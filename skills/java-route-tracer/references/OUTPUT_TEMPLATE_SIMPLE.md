# 路由调用链追踪报告（简化版） — 填充式输出模板

> **硬约束（不可违反）：**
> 1. 不得增删章节 — 模板有 3 个章节，输出必须有 3 个章节
> 2. 不得调整章节顺序
> 3. 所有【填写】占位符必须替换为实际内容，不得保留
> 4. **适用于第 2 个及之后的接口**
> 5. 必须独立追踪该方法的调用链到 Sink，禁止复用其他方法的结论
> 6. 文件命名格式: `{project_name}_trace_{method_name}_{YYYYMMDD_HHMMSS}.md`
>
> 参考: shared/OUTPUT_STANDARD.md

---

## 以下为完整输出模板，直接填充生成

---

# 路由调用链追踪报告（简化版）

**追踪路由**: 【填写：路由路径】
**方法**: 【填写：方法名】
**生成时间**: 【填写：YYYY-MM-DD HH:MM:SS】

---

## 1. HTTP 请求数据包

### 1.1 完整请求模板

```http
【填写：该方法的完整 HTTP 请求模板】
```

### 1.2 该方法参数定义

**参数来源**: 从 【填写：ClassName.method()】 方法签名读取

#### 顶层参数

| 参数名 | Java类型 | 说明 |
|:-------|:---------|:-----|
| 【填写】 | 【填写】 | 【填写】 |

#### 参数内部结构（如有）

| 字段名 | Java类型 | 说明 |
|:-------|:---------|:-----|
| 【填写】 | 【填写】 | 【填写】 |

<!-- 若参数无内部结构，填写"无复杂参数结构" -->

### 1.3 测试用数据包示例

```http
【填写：可直接使用的测试请求】
```

---

## 2. 调用链追踪

### 调用链层级

```
[L1] 【填写：Class.method()】
    ↓
[L2] 【填写：Class.method()】
    ↓
[Sink] 【填写：Sink类型: 具体位置；若无敏感 Sink 填"无敏感 Sink"】
```

### Sink 识别

| Sink 类型 | 位置 | 说明 |
|:----------|:-----|:-----|
| 【填写：SQL/COMMAND/HTTP/FILE/XML/无敏感Sink】 | 【填写：Class.java:line】 | 【填写：具体操作】 |

### 参数传递关系

| 参数 | L1 | L2 | ... | 最终使用 |
|:-----|:---|:---|:----|:---------|
| 【填写】 | 【填写】 | 【填写】 | ... | 【填写】 |

---

## 3. 可控性判定

| 参数 | Sink类型 | 覆盖类型 | 覆盖条件 | 可控性结论 | 可控场景 |
|:-----|:---------|:---------|:---------|:-----------|:---------|
| 【填写】 | 【填写】 | 【填写】 | 【填写】 | 【填写：✅/⚠️/❌/-】 | 【填写】 |

**可控性汇总**:

| 类型 | 参数 | 审计要求 |
|:-----|:-----|:---------|
| ✅ 完全可控 | 【填写；若无填"无"】 | 需审计 Sink 安全性 |
| ⚠️ 条件可控 | 【填写；若无填"无"】 | 需验证绕过条件 |
| ❌ 不可控 | 【填写；若无填"无"】 | 可排除漏洞可能 |
| - 无敏感Sink | 【填写；若无填"无"】 | 低风险 |

---

## 输出自检（生成文件后必须逐项确认）

- [ ] 文件名符合 `{project_name}_trace_{method_name}_{YYYYMMDD_HHMMSS}.md`
- [ ] 所有【填写】占位符已替换为实际内容
- [ ] 参数定义从该方法实际代码读取（非假设）
- [ ] 独立追踪了该方法的调用链到 Sink（未复用其他方法结论）
- [ ] Sink 类型正确识别
- [ ] 可控性判定完整
- [ ] 章节数量为 3 个，顺序与模板一致
