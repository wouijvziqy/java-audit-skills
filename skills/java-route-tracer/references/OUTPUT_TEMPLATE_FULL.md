# 路由调用链追踪报告（完整版） — 填充式输出模板

> **硬约束（不可违反）：**
> 1. 不得增删章节 — 模板有 7 个章节，输出必须有 7 个章节
> 2. 不得调整章节顺序
> 3. 所有【填写】占位符必须替换为实际内容，不得保留
> 4. 每个层级必须包含：文件位置、类名、方法签名、关键代码、参数传递
> 5. **适用于第 1 个接口的完整追踪，后续接口使用 OUTPUT_TEMPLATE_SIMPLE.md**
> 6. 文件命名格式: `{project_name}_trace_{method_name}_{YYYYMMDD_HHMMSS}.md`
>
> 参考: shared/OUTPUT_STANDARD.md

---

## 以下为完整输出模板，直接填充生成

---

# 路由调用链追踪报告

**追踪路由**: 【填写：路由路径，如 /admin/image/getImageCapture.action】
**生成时间**: 【填写：YYYY-MM-DD HH:MM:SS】
**项目路径**: 【填写：项目源码路径】

---

## 1. HTTP 请求数据包

### 1.1 完整请求模板

```http
【填写：完整的 HTTP 请求，包含 Method、路径、Host、Content-Type、Cookie、请求体】
```

### 1.2 参数详细定义

#### 顶层参数

| 参数名 | Java类型 | HTTP位置 | 必填 | 说明 |
|:-------|:---------|:---------|:-----|:-----|
| 【填写】 | 【填写】 | 【填写：Body/Query/Path/Header】 | 【填写：是/否】 | 【填写】 |

<!-- 按实际参数数量填写 -->

#### 【填写：参数名】 内部结构 (【填写：Java类型】)

| 字段名 | Java类型 | 说明 | 最终使用位置 |
|:-------|:---------|:-----|:-------------|
| 【填写】 | 【填写】 | 【填写】 | 【填写：如"AbstractDao.findSql():234 - SQL拼接"；若无敏感使用填"-"】 |

<!-- 如有多个复杂参数，重复此结构 -->

### 1.3 测试用数据包示例

```http
【填写：可直接使用的测试请求，参数填充真实示例值】
```

---

## 2. 调用链层级追踪

<!-- 以下区块按实际调用层级数量重复 -->

### [Level 【填写：层级号】] 【填写：层名称，如 Action 入口层 / Manager 服务层 / DAO 数据访问层】

**文件**: 【填写：完整包路径/文件名:行号】
**类名**: 【填写：类名（含父类信息）】
**方法签名**:
```java
【填写：方法签名】
```

**完整代码:**
```java
【填写：粘贴方法完整代码，保留原始缩进】
```

**参数转换/传递:**

| 接收参数 | 类型 | 传递到下一层 |
|:---------|:-----|:-------------|
| 【填写】 | 【填写】 | 【填写：✅ / ❌ (未传递)】 |

**下一层调用**: 【填写：如 this.xxxManager.xxxMethod(param1, param2)】

<!-- 重复区块结束 -->

---

## 3. 调用链总结图

```
【填写：从 HTTP Request 到最终 Sink 的完整 ASCII 图，标注参数流向和变量名变化】
```

---

## 4. 参数变量名追踪表（核心）

<!-- 对每个到达 Sink 的参数生成一个追踪表 -->

### 4.【填写：序号】 【填写：参数路径，如 pageJson.orderBy】 参数追踪

| 层级 | 类名 | 变量名 | 类型 | 代码位置 | 说明 |
|:-----|:-----|:-------|:-----|:---------|:-----|
| HTTP | - | 【填写：HTTP 参数名】 | String | 请求Body/Query | 原始输入 |
| L1 | 【填写】 | 【填写】 | 【填写】 | :【填写：行号】 | 【填写】 |
| L【填写：N】 | 【填写】 | 【填写】 | 【填写】 | :【填写：行号】 | **最终使用: 【填写：如 SQL拼接】** |

<!-- 对每个到达 Sink 的参数重复 -->

### 完整参数追踪汇总

| 原始HTTP参数 | L1变量名 | L2变量名 | ... | 最终使用 |
|:-------------|:---------|:---------|:----|:---------|
| 【填写】 | 【填写】 | 【填写】 | ... | 【填写】 |

---

## 5. 参数实际使用检查

| 参数 | Sink类型 | 覆盖类型 | 覆盖条件 | 可控性结论 | 可控场景 |
|:-----|:---------|:---------|:---------|:-----------|:---------|
| 【填写】 | 【填写：SQL/COMMAND/HTTP/FILE/XML/LDAP/EXPRESSION/DESERIALIZE/RESPONSE/PATH】 | 【填写：无覆盖/无条件覆盖/条件覆盖】 | 【填写：覆盖条件；若无覆盖填"-"】 | 【填写：✅ 完全可控 / ⚠️ 条件可控 / ❌ 不可控】 | 【填写：可控场景描述】 |

---

## 6. 参数可控性判定

### 可控性汇总

| 类型 | 参数 | 审计要求 |
|:-----|:-----|:---------|
| ✅ 完全可控 | 【填写：参数列表；若无填"无"】 | 必须审计 Sink 安全性 |
| ⚠️ 条件可控 | 【填写：参数列表；若无填"无"】 | 需验证绕过条件 |
| ❌ 不可控 | 【填写：参数列表；若无填"无"】 | 可排除漏洞可能 |

---

## 7. 分支条件追踪

<!-- 如有条件分支 -->

```
【填写：方法名】(param)
    ├─ [if 【填写：条件】] → 【填写：分支执行内容】
    │      └─→ 【填写：敏感操作 / 无敏感操作】
    └─ [else] → 【填写：默认分支内容】
```

### 执行路径结论

| 路径 | 条件 | 敏感操作 | 可利用性 |
|:-----|:-----|:---------|:---------|
| 【填写】 | 【填写】 | 【填写：✅ 执行 / ❌ 不执行】 | 【填写：可利用 / 不可利用】 |

<!-- 若无分支填写"本调用链无条件分支" -->

---

## 输出自检（生成文件后必须逐项确认）

- [ ] 文件名符合 `{project_name}_trace_{method_name}_{YYYYMMDD_HHMMSS}.md`
- [ ] 所有【填写】占位符已替换为实际内容
- [ ] 每个调用层级都有：文件位置 + 类名 + 方法签名 + 完整代码 + 参数传递表
- [ ] "3. 调用链总结图"完整展示了从 HTTP 到 Sink 的流向
- [ ] "4. 参数变量名追踪表"覆盖了所有到达 Sink 的参数
- [ ] "5. 参数实际使用检查"检查了硬编码覆盖
- [ ] "6. 可控性判定"给出了每个参数的结论
- [ ] "7. 分支条件追踪"记录了所有条件分支
- [ ] 章节数量为 7 个，顺序与模板一致
