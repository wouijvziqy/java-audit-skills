# SQL 注入审计报告 — 填充式输出模板

> **硬约束（不可违反）：**
> 1. 不得增删章节 — 模板有 6 个章节，输出必须有 6 个章节
> 2. 不得调整章节顺序
> 3. 不得修改表格列名和列数
> 4. 所有【填写】占位符必须替换为实际内容，不得保留
> 5. 不得自由发挥添加额外章节
> 6. 文件命名格式: `{project_name}_sql_audit_{YYYYMMDD_HHMMSS}.md`
>
> 参考: shared/OUTPUT_STANDARD.md

---

## 以下为完整输出模板，直接填充生成

---

# 【填写：项目名称】 - SQL 注入审计报告

生成时间: 【填写：YYYY-MM-DD HH:MM:SS】
分析路径: 【填写：项目源码路径】

---

## 1. 审计概述

| 项目 | 信息 |
|------|------|
| 审计范围 | 【填写：项目源码路径】 |
| SQL 框架 | 【填写：JDBC / MyBatis / Hibernate，可多选】 |
| 分析方法 | 静态代码审计 + 数据流分析 |

---

## 2. 风险统计

| 严重等级 | CVSS | 数量 | 说明 |
|----------|------|------|------|
| 🔴 C (Critical) | 9.0-10.0 | 【填写】 | 可直接导致系统沦陷 |
| 🟠 H (High) | 7.0-8.9 | 【填写】 | 可造成重大损害 |
| 🟡 M (Medium) | 4.0-6.9 | 【填写】 | 可造成一定损害 |
| 🔵 L (Low) | 0.1-3.9 | 【填写】 | 安全加固建议 |

---

## 3. SQL 操作映射表

| 序号 | 类名 | 方法 | 框架 | SQL类型 | 参数化状态 | 可利用性 |
|------|------|------|------|---------|------------|----------|
| 【填写：序号】 | 【填写：类名】 | 【填写：方法名】 | 【填写：JDBC/MyBatis/Hibernate】 | 【填写：SELECT/INSERT/UPDATE/DELETE】 | 【填写：✅ 安全 / ❌ 危险】 | 【填写：⚠️ 待验证 / ✅ 已确认 / ❌ 不可利用 / - 】 |

<!-- 按实际 SQL 操作数量填写，每个操作一行，不得省略 -->

---

## 4. 高危风险详情

<!-- 以下区块按实际漏洞数量重复，每个漏洞一个区块 -->

### 【填写：漏洞编号，格式 {C/H/M/L}-SQL-{序号}】 【填写：风险标题】

| 项目 | 信息 |
|------|------|
| 严重等级 | 【填写：🔴/🟠/🟡/🔵 + Critical/High/Medium/Low + (CVSS 分数)】 |
| 可达性 (R) | 【填写：0-3 + 判定理由】 |
| 影响范围 (I) | 【填写：0-3 + 判定理由】 |
| 利用复杂度 (C) | 【填写：0-3 + 判定理由】 |
| 可利用性 | 【填写：✅ 已确认 / ⚠️ 待验证 / ❌ 不可利用 / 🔍 环境依赖】 |
| 位置 | 【填写：ClassName.method (file:line)】 |
| 框架 | 【填写：JDBC / MyBatis / Hibernate】 |

#### 执行条件分析

| 项目 | 值 |
|------|-----|
| 分支条件 | 【填写：代码中的分支判断条件，如 `if (this.isOracle())`；若无分支填 `无`】 |
| 分支1 | 【填写：分支描述及 SQL 行为】 |
| 分支2 | 【填写：分支描述及 SQL 行为；若无第二分支填 `N/A`】 |
| 目标环境 | 【填写：MySQL / Oracle / 未知】 |
| **结论** | 【填写：已确认可利用 / 不可利用 / 需确认环境】 |

#### 漏洞代码

```java
【填写：粘贴危险代码片段，保留原始缩进】
```

#### 数据流分析

```
【填写：从用户输入到 SQL 执行的完整数据流路径，格式如下】
用户输入: 【填写：Controller 入口方法】
     ↓
参数传递: 【填写：Service 层调用链】
     ↓
SQL 执行: 【填写：DAO/Mapper 方法及 SQL 语句】
```

#### 验证 PoC

```http
【填写：可直接在 Burp Suite 中使用的 HTTP 请求】
```

#### 建议修复

```java
【填写：修复后的安全代码】
```

<!-- 重复区块结束 -->

---

## 5. 验证 Payload 参考

| 注入类型 | 测试 Payload | 预期结果 |
|----------|--------------|----------|
| 基于错误 | `'` | 数据库错误信息 |
| 布尔盲注 | `1' AND '1'='1` | 正常响应 |
| 时间盲注 | `1' AND SLEEP(5)--` | 延迟响应 |
| UNION 注入 | `1' UNION SELECT 1,2,3--` | 额外数据 |

---

## 6. 审计结论

| 统计项 | 数量 |
|--------|------|
| 总 SQL 操作数 | 【填写】 |
| 安全操作 | 【填写】 |
| 危险操作 | 【填写】 |
| 可利用漏洞 | 【填写】 |
| 环境依赖漏洞 | 【填写】 |

---

## 输出自检（生成文件后必须逐项确认）

- [ ] 文件名符合 `{project_name}_sql_audit_{YYYYMMDD_HHMMSS}.md`
- [ ] 所有【填写】占位符已替换为实际内容
- [ ] 包含"1. 审计概述"表格
- [ ] 风险统计表有 C/H/M/L 四行，数量与正文漏洞数一致
- [ ] "3. SQL 操作映射表"列出了所有 SQL 操作，无遗漏
- [ ] 每个漏洞有"执行条件分析"表格（即使无分支也要填"无"）
- [ ] 每个漏洞有"数据流分析"（从 Controller 到 SQL 执行）
- [ ] 每个漏洞有"验证 PoC"（可直接使用的 HTTP 请求）
- [ ] "6. 审计结论"统计数据与正文一致
- [ ] 章节数量为 6 个，顺序与模板一致
