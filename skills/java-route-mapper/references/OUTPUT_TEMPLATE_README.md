# 路由识别教程 — 填充式输出模板

> **硬约束（不可违反）：**
> 1. 不得增删章节 — 模板有 6 个章节，输出必须有 6 个章节
> 2. 不得调整章节顺序
> 3. 所有【填写】占位符必须替换为实际内容，不得保留
> 4. 文件命名格式: `{project_name}_route_README_{YYYYMMDD_HHMMSS}.md`
> 5. 只包含本项目实际使用的框架的识别教程，不要列出未使用的框架
>
> 参考: shared/OUTPUT_STANDARD.md

---

## 以下为完整输出模板，直接填充生成

---

# 【填写：项目名称】 - 路由识别教程

生成时间: 【填写：YYYY-MM-DD HH:MM:SS】

## 1. 本文档说明

本文档说明如何从 【填写：项目名称】 的源码中识别 HTTP 路由和请求参数。

---

## 2. 使用的框架

本项目使用了以下框架：

<!-- 只列出本项目实际使用的框架，以下为各框架的模板，按需保留 -->

### 【填写：框架名称，如 Spring MVC / Struts 2 / CXF Web Service / JAX-RS / Servlet】

**识别方法：**
- 【填写：如何识别该框架的存在】

**路由组成：**
- 【填写：路由 URL 如何拼接】

**参数识别：**

| 注解/方式 | 参数来源 | 示例 |
|:----------|:--------|:-----|
| 【填写】 | 【填写】 | 【填写】 |

<!-- 按实际使用的框架数量重复此区块 -->

---

## 3. 参数类型说明

### 基本类型

| Java 类型 | HTTP 表示 | 示例 |
|:--------|:--------|:-----|
| String | 文本 | `"value"` |
| int/Integer | 整数 | `123` |
| long/Long | 长整数 | `123456789` |
| boolean/Boolean | 布尔值 | `true` |

### 集合类型

| Java 类型 | HTTP 表示 | 示例 |
|:--------|:--------|:-----|
| List/Array | JSON 数组 | `["a", "b", "c"]` |
| Map | JSON 对象 | `{"key": "value"}` |

### 对象类型 (POJO)

- 反序列化为嵌套的 JSON 对象
- 字段名与方法对应

---

## 4. 反编译使用说明

【填写：本项目是否使用了反编译；若未使用填写"本项目源码完整，未使用反编译"】

---

## 5. 常见问题

**Q: 为什么某些路由无法解析？**
A: 可能原因：
- 使用了动态路由（如通配符 `*`）
- 使用了自定义拦截器/过滤器
- 配置使用了表达式（如 SpEL）

**Q: 参数类型显示为 `unknown`？**
A: 使用反编译工具获取完整的类定义

---

## 6. 相关文档

- 主索引: 【填写：主索引文件名及链接】
- 【填写：各模块详情文件名及链接，每个一行】

---

## 输出自检（生成文件后必须逐项确认）

- [ ] 文件名符合 `{project_name}_route_README_{YYYYMMDD_HHMMSS}.md`
- [ ] 所有【填写】占位符已替换为实际内容
- [ ] 只列出了本项目实际使用的框架（不列未使用的）
- [ ] "6. 相关文档"链接指向正确的主索引和详情文件
- [ ] 章节数量为 6 个，顺序与模板一致
