# 质检员校验报告模板

本文件定义质检员（agent-7-x）在每个阶段的**强制填充式校验报告模板**。

质检员完成校验后，必须按照对应模板**逐项填写**所有字段，禁止省略、合并或用一句话概括。每个 `{填写}` 占位符都必须替换为实际内容。

---

## 目录

- [通用报告结构](#通用报告结构)
- [阶段1 校验清单](#阶段1-校验清单)
  - [agent-1 route-mapper](#agent-1-route-mapper-校验清单)
  - [agent-2 auth-audit](#agent-2-auth-audit-校验清单)
  - [agent-3 vuln-scanner](#agent-3-vuln-scanner-校验清单)
- [阶段2 校验清单](#阶段2-校验清单)
  - [agent-4a risk-classifier](#agent-4a-risk-classifier-校验清单)
  - [agent-4b vuln-aggregator](#agent-4b-vuln-aggregator-校验清单)
- [阶段3 校验清单](#阶段3-校验清单)
  - [agent-5-N route-tracer worker](#agent-5-n-route-tracer-worker-校验清单)
- [阶段4 校验清单](#阶段4-校验清单)
  - [agent-6x 漏洞审计通用清单](#agent-6x-漏洞审计通用校验清单)

---

## 通用报告结构

每份校验报告必须包含以下三个部分，缺一不可：

````markdown
# 校验报告: {被校验 agent 名称}

## 基本信息
- 质检员: agent-7-{N}
- 校验对象: {被校验 agent 名称}
- 对应 Skill: {加载的 skill 名称}
- 校验文件: {实际读取的输出文件路径，多个用逗号分隔}

## 逐项校验结果

{使用下方对应阶段的校验清单表格，逐行填写}

## 最终判定
- 状态: **通过** / **不通过**
- 通过项: {M}/{N}
- 不通过项清单（如有）:
  - {校验项编号}. {校验项名称}: {具体缺失内容及修复要求}
````

---

## 阶段1 校验清单

### agent-1-route-mapper 校验清单

校验前先使用 Skill 工具加载 `/java-route-mapper`，从 skill 上下文中提取输出规范。

| # | 校验项 | 预期 | 实际 | 状态 |
|---|--------|------|------|------|
| 1 | 输出文件存在且非空 | 文件存在，内容长度 > 0 | {填写文件路径及大小} | {✅/❌} |
| 2 | 路由列表表格 | Markdown 表格，列含路由路径、HTTP 方法、Controller 方法名 | {填写：表格是否存在，行数} | {✅/❌} |
| 3 | 参数结构文档 | 每个路由标注参数名、类型、来源（@RequestParam/@PathVariable/@RequestBody 等） | {填写：是否覆盖所有路由的参数} | {✅/❌} |
| 4 | Burp Suite 请求模板 | 每个路由对应一个完整 HTTP 请求模板 | {填写：模板数量 vs 路由数量} | {✅/❌} |
| 5 | 源文件位置标注 | 含 Java 源文件路径 + 行号 | {填写：是否每个路由都有文件位置} | {✅/❌} |

---

### agent-2-auth-audit 校验清单

校验前先使用 Skill 工具加载 `/java-auth-audit`，从 skill 上下文中提取输出规范。

| # | 校验项 | 预期 | 实际 | 状态 |
|---|--------|------|------|------|
| 1 | 输出文件存在且非空 | 文件存在，内容长度 > 0 | {填写文件路径及大小} | {✅/❌} |
| 2 | 鉴权框架识别 | 明确识别鉴权框架类型（Shiro/Spring Security/JWT/Filter/自定义等） | {填写：识别出的框架类型} | {✅/❌} |
| 3 | 组件版本检查 | 鉴权相关组件的版本号及已知漏洞 | {填写：是否包含版本信息} | {✅/❌} |
| 4 | 路由鉴权映射表 | 表格含 ✅/⚠️/❌ 状态标记，覆盖 agent-1 输出的路由 | {填写：映射表行数} | {✅/❌} |
| 5 | 风险统计章节 | 包含无鉴权路由数、弱鉴权路由数、风险占比 | {填写：统计数据是否完整} | {✅/❌} |
| 6 | 高危详情 + PoC | 每个高危项有详细说明和验证步骤 | {填写：高危项数量及 PoC 有无} | {✅/❌} |

**覆盖率校验（关键项）：**

| 指标 | 实际值 | 阈值 | 状态 |
|------|--------|------|------|
| 路由鉴权映射覆盖率 | {填写：映射表路由数} / {填写：agent-1 路由总数} = {填写}% | >= 80% | {✅/❌} |

---

### agent-3-vuln-scanner 校验清单

校验前先使用 Skill 工具加载 `/java-vuln-scanner`，从 skill 上下文中提取输出规范。

| # | 校验项 | 预期 | 实际 | 状态 |
|---|--------|------|------|------|
| 1 | 输出文件存在且非空 | 文件存在，内容长度 > 0 | {填写文件路径及大小} | {✅/❌} |
| 2 | 扫描概览 | 包含扫描范围、依赖总数、检出漏洞数 | {填写：概览章节是否存在及内容} | {✅/❌} |
| 3 | 模块风险摘要 | 按模块/目录分组的风险等级汇总 | {填写：是否有分组摘要} | {✅/❌} |
| 4 | 漏洞详情（CVE） | 每个漏洞含 CVE 编号、影响组件、版本、风险等级 | {填写：漏洞条目数} | {✅/❌} |
| 5 | 触发点检测章节 | 标注漏洞组件在代码中的实际调用位置 | {填写：是否有触发点信息} | {✅/❌} |

---

## 阶段2 校验清单

### agent-4a-risk-classifier 校验清单

| # | 校验项 | 预期 | 实际 | 状态 |
|---|--------|------|------|------|
| 1 | `high_risk_routes.md` 存在且非空 | 文件存在，内容长度 > 0 | {填写文件路径及大小} | {✅/❌} |
| 2 | 优先级分组表格 | 包含 P0 和 P1 分组，每组有路由列表 | {填写：P0 数量、P1 数量} | {✅/❌} |
| 3 | 待追踪路由列表 | 明确列出需要调用链追踪的路由 | {填写：待追踪路由数} | {✅/❌} |
| 4 | P0 交叉验证 | P0 路由同时满足"无鉴权"（来自 agent-2）+"有漏洞触发点"（来自 agent-3） | {填写：逐条验证结果} | {✅/❌} |
| 5 | 追踪理由无 CVE 编号 | 追踪理由字段不包含具体 CVE-XXXX-XXXXX 编号 | {填写：grep CVE 结果} | {✅/❌} |

---

### agent-4b-vuln-aggregator 校验清单

| # | 校验项 | 预期 | 实际 | 状态 |
|---|--------|------|------|------|
| 1 | `component_vulnerabilities.md` 存在且非空 | 文件存在，内容长度 > 0 | {填写文件路径及大小} | {✅/❌} |
| 2 | 组件漏洞概览 | 含高危/中危数量统计 | {填写：统计章节是否存在} | {✅/❌} |
| 3 | 组件漏洞详情 | 每个漏洞含影响路由和 PoC | {填写：漏洞条目数} | {✅/❌} |
| 4 | `auth_bypass_vulnerabilities.md` 存在且非空 | 文件存在，内容长度 > 0 | {填写文件路径及大小} | {✅/❌} |
| 5 | 代码层鉴权绕过 | 包含来自 agent-2 的鉴权绕过漏洞 | {填写：是否引用 agent-2 数据} | {✅/❌} |
| 6 | 组件导致的鉴权绕过 | 包含来自 agent-3 的组件漏洞导致的鉴权绕过 | {填写：是否引用 agent-3 数据} | {✅/❌} |

---

## 阶段3 校验清单

### agent-5-N route-tracer worker 校验清单

校验前先使用 Skill 工具加载 `/java-route-tracer`，从 skill 上下文中提取输出规范。

| # | 校验项 | 预期 | 实际 | 状态 |
|---|--------|------|------|------|
| 1 | 输出文件存在且非空 | 该 worker 负责的所有路由均有对应输出文件 | {填写：文件数量 vs 预期数量} | {✅/❌} |
| 2 | HTTP 请求数据包 | 每个路由追踪报告包含完整 HTTP 请求 | {填写：是否每份报告都有请求数据包} | {✅/❌} |
| 3 | 层级调用链 | Controller → Service → DAO 的逐层调用记录，含文件路径和方法签名 | {填写：调用链层级深度} | {✅/❌} |
| 4 | 参数追踪表 | 参数从入口到底层的传递路径表格 | {填写：是否每个路由都有参数追踪} | {✅/❌} |
| 5 | 执行路径结论 | 每条追踪有明确的路径总结 | {填写：结论章节是否存在} | {✅/❌} |
| 6 | 鉴权状态判定章节 | 必须包含：鉴权状态标注、鉴权绕过漏洞（如有）、风险等级判定 | {填写：三个子项是否齐全} | {✅/❌} |
| 7 | 无未替换变量 | 输出中不含 `${...}` 占位符 | {填写：grep `${` 结果，0 matches 为通过} | {✅/❌} |
| 8 | 文件数量匹配 | 文件数 = 追踪方法数 + 1（总索引文件） | {填写：实际文件数} / {填写：预期文件数} | {✅/❌} |

**覆盖率校验（全部 worker 通过后由负责人汇总）：**

| 指标 | 实际值 | 阈值 | 状态 |
|------|--------|------|------|
| 追踪覆盖率 | {填写：已追踪数} / {填写：high_risk 待追踪数 P0+P1} = {填写}% | >= 90% | {✅/❌} |

---

## 阶段4 校验清单

### agent-6x 漏洞审计通用校验清单

适用于 agent-6a（SQL）、agent-6b（XXE）、agent-6c（文件上传）、agent-6d（文件读取）。

校验前先使用 Skill 工具加载对应 skill（`/java-sql-audit`、`/java-xxe-audit`、`/java-file-upload-audit`、`/java-file-read-audit`），从 skill 上下文中提取输出规范。

| # | 校验项 | 预期 | 实际 | 状态 |
|---|--------|------|------|------|
| 1 | 输出文件存在且非空 | 审计报告文件存在，内容长度 > 0 | {填写文件路径及大小} | {✅/❌} |
| 2 | 报告结构符合 skill 输出规范 | 包含 skill 定义的所有必需章节 | {填写：已有章节 vs 必需章节} | {✅/❌} |
| 3 | 漏洞条目完整性 | 每个漏洞含：漏洞类型、风险等级、触发位置、调用链引用 | {填写：漏洞条目数及字段完整性} | {✅/❌} |
| 4 | 可利用前置条件章节 | 每个漏洞报告必须包含此章节，含以下四个子项 | {填写：是否存在此章节} | {✅/❌} |
| 4a | - 鉴权要求 | 明确标注访问该漏洞需要的鉴权条件 | {填写：具体内容} | {✅/❌} |
| 4b | - 鉴权绕过方式 | 如存在绕过方式则列出，不存在则明确标注"无" | {填写：具体内容} | {✅/❌} |
| 4c | - 其他前置条件 | 特殊配置、环境依赖等 | {填写：具体内容} | {✅/❌} |
| 4d | - 综合可利用性判定 | 明确结论：可直接利用 / 需满足条件后可利用 / 不可利用 | {填写：判定结论} | {✅/❌} |

**覆盖率校验（全部 agent-6x 通过后由负责人汇总）：**

| 指标 | 实际值 | 阈值 | 状态 |
|------|--------|------|------|
| 漏洞审计覆盖率 | {填写：已审计调用链数} / {填写：含 sink 的调用链总数} = {填写}% | >= 90% | {✅/❌} |

---

## 最终质量报告模板

全部 agent-6x 校验通过后，质检员整合所有阶段的校验结果，按以下模板生成 `{output_path}/quality_report.md`：

````markdown
# 审计质量检查报告

## 总览
| 阶段 | Skill | 状态 | 通过项/总项 | 重做次数 |
|:-----|:------|:-----|:-----------|:---------|
| {填写} | {填写} | {✅/❌→✅} | {填写} | {填写} |

## 各阶段校验详情

### 阶段1
- {✅/❌→✅} java-route-mapper: {M}/{N} 项通过
- {✅/❌→✅} java-auth-audit: {M}/{N} 项通过（重做{X}次）
- {✅/❌→✅} java-vuln-scanner: {M}/{N} 项通过

### 阶段2
- {✅/❌→✅} agent-4a-risk-classifier: high_risk_routes.md 校验{通过/不通过}
- {✅/❌→✅} agent-4b-vuln-aggregator: 组件漏洞汇总+鉴权绕过汇总 校验{通过/不通过}

### 阶段3
- {✅/❌→✅} agent-5-route-tracer: 分批方案校验{通过/不通过}（P0+P1 全覆盖，{N} 批次）
- {✅/❌→✅} agent-5-1: java-route-tracer 校验{通过/不通过}（含鉴权状态透传）[agent-7-{X}]
- {✅/❌→✅} agent-5-2: java-route-tracer 校验{通过/不通过}（含鉴权状态透传）[agent-7-{X}]
- ...（逐个 worker 校验记录）
- {✅/❌} 追踪覆盖率: {X}% >= 90%

### 阶段4
- {✅/❌→✅} java-sql-audit: {M}/{N} 项通过（含可利用前置条件）
- {✅/❌→✅} java-xxe-audit: {M}/{N} 项通过（含可利用前置条件）
- {✅/❌→✅} java-file-upload-audit: {M}/{N} 项通过（含可利用前置条件）
- {✅/❌→✅} java-file-read-audit: {M}/{N} 项通过（含可利用前置条件）

## 数据一致性
| 校验项 | 实际值 | 阈值 | 状态 |
|:-------|:-------|:-----|:-----|
| 路由覆盖率 | {填写}% | 80% | {✅/❌} |
| 高危路由追踪率 | {填写}% | 90% | {✅/❌} |
| 漏洞审计覆盖率 | {填写}% | 90% | {✅/❌} |

## 审计统计汇总
| 指标 | 数量 |
|:-----|:-----|
| 总路由数 | {填写} |
| 无鉴权路由数 | {填写} |
| 组件漏洞数 | {填写} |
| 鉴权绕过漏洞数 | {填写} |
| 高危路由数 | {填写} |
| 已追踪调用链数 | {填写} |
| 发现代码漏洞数 | {填写} |
````
